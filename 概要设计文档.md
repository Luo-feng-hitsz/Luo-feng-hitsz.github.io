# 个人网站重构概要设计

## 技术方案

- 静态网站生成器：Hexo。
- 主题：Fluid，通过 npm 依赖安装。
- 内容格式：Markdown，启用文章资源目录管理本地图片。
- 托管平台：GitHub Pages。
- 自动化：后续使用 GitHub Actions 安装依赖、执行构建并发布静态产物。

## 目录结构

```text
person-site/
├─ .github/             自动化配置
├─ scaffolds/           文章模板
├─ source/
│  ├─ _posts/           Markdown 文章
│  ├─ about/            关于页面
│  ├─ projects/         项目页面
│  └─ images/           公共本地图片
├─ _config.yml          Hexo 站点配置
├─ _config.fluid.yml    Fluid 主题配置
├─ package.json
├─ package-lock.json
└─ .gitignore
```

## 配置设计

- `_config.yml` 保存站点名称、域名、语言、时区、永久链接和主题选择。
- `_config.fluid.yml` 保存 Fluid 的导航、首页、文章、关于页与视觉配置。
- 主题本体由 `hexo-theme-fluid` npm 包提供，不直接复制进源码仓库。
- `post_asset_folder` 开启，迁移文章时将图片与文章放在同名资源目录中。

## 开发与发布流程

1. 本地安装依赖并启动预览。
2. 完成个人信息、页面和旧内容迁移。
3. 执行 `npm run check` 完成干净构建自测。
4. 推送到重构分支并检查自动构建。
5. 验收通过后切换 GitHub Pages，并保留旧版本回退点。

## 质量控制

- 每次发布前执行构建、链接和图片检查。
- 用户测试发现的问题记录到 `TEST-ISSUES.md`，并纳入后续回归测试。
- 线上切换前验证首页、文章、项目、关于、404、移动端与深色模式。
