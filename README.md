# Luo Feng 个人网站

基于 Hexo 与 Fluid 主题构建的个人主页和技术博客。

## 本地开发

```bash
npm install
npm run server
```

默认访问地址：`http://localhost:4000/`。

## 构建检查

```bash
npm run check
```

生成的静态文件位于 `public/`，该目录不提交到 Git。

## 常用命令

```bash
npx hexo new "文章标题"
npm run clean
npm run build
```
